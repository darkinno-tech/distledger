# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 与 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式。

> **提交信息与代码注释统一使用英文**（面向国际社区与 godoc）。
> 本文档与 PRD、ADR 保持中文，因为它们记录的是设计意图与取舍，读者是维护者。
>
> **API 稳定性承诺**：`v1.0` 之前允许破坏性变更，但必须记录在本文件；`v1.0` 之后遵循语义化版本。

---

## [Unreleased]

### Planned
- v0.4 MySQL Store（事务、行锁、`Migrate`、`schema.sql`）
- v0.5 提现链路与 `PayoutChannel`（含「已出账后追回」的欠款策略）
- v0.6 可观测性钩子 + `examples` 补齐 02–05

---

## [0.3.0] - 2026-09-14

退款冲正、风控冻结与作废，以及一轮对抗性审查发现问题的修复。

### Added

**退款冲正**
- `OnOrderRefunded`：支持整单全额（`IsFull`）、按明细（`Items`）、整单部分（`Amount`）三种表达，三者互斥
- 冲正以**负额佣金记录**追加，`Commission.ReverseOf` 指回原始记录；原始记录的财务字段保持不可变
- `Commission.ReversedAmount`：已被冲正的累计额（生命周期字段，带版本 CAS）
- `Account.TotalReversed`：累计冲正额，与 `TotalEarned` 配对得到净收益
- 按明细、按批次、任意顺序的退款；单次退款可跨多个计佣单位按基数占比自动分摊
- `Refund` 退款凭证实体：记录每次退款批次、金额与**该明细的累计退款额**
- 不变量 **I3**：冲正累计额 ↔ 冲正明细 ↔ 账户毛额/冲正额三方对账
- `Bucket` 类型：资金桶的唯一一处定义，被负余额守卫、I1 与流水构造共同使用

**风控**
- `FreezeCommission` / `UnfreezeCommission`：让 `Frozen` 状态真正可达（此前只存在于迁移表中）
- `VoidCommission`：没收一笔佣金——全额追回并落到终态 `Void`，流水业务类型为 `LedgerVoid`
- 状态机新增 `Frozen → Reversed`：风控冻结中的佣金，其钱仍在待结算桶里，退款时必须能拿回来

**运维**
- `MaintainResult.Quarantined`：数据异常的佣金被隔离并上报，而不是中止整批结算
- `Maintain` 按租户隔离失败：一个租户出错不再阻塞其它租户
- `Logger` 真正被使用（冻结、解冻、作废、心跳汇总、隔离告警），此前是零调用点的空壳

### Changed（破坏性）

- **`Maintain(ctx)`**：去掉 `now` 参数。处理时间一律来自注入的 `Clock`——允许调用方推进时间就等于允许他们提前结算。事件时间（`PaidAt`、`ReceivedAt`、`RefundedAt`）仍由调用方提供，因为那是业务事实（见 ADR-023）
- **`OrderRefundedEvent.IdemKey` 必填**：没有它，「同一次退款被重投」与「两次金额相同的真实退款」在数据上完全等价（见 ADR-024）
- **`EligibilityChecker.Eligible` 改签名**为 `Eligible(ctx, EligibilityInput)`，输入中新增 `BuyerUserID`，使自购判定可以被表达
- **`AccrueResult.AccruedAmount()` 返回 `(Money, error)`**，不再静默回绕
- 明细金额必须 > 0（0 元明细既不能产生佣金，又曾是最廉价的放大工作量手段）
- 同一订单号的第二个支付事件若声称**不同的买家**，现在返回错误而不是被当成重放成功

### Fixed（审查驱动的结构性修复）

- **`TotalEarned` / `TotalReversed` 落在不变量之外**：v0.1 的每个检查都手写字段名，导致新资金字段
  静默地不受任何约束。引入 `Bucket` 类型把资金桶定义收敛到唯一一处，`Settleable()` 与 I1 都遍历它，
  流水构造也由它驱动；另有一个反射测试强制「`Account` 上每个 `Money` 字段要么是桶、要么登记在豁免表里」
- **重试的定义被写死**：原文只说「可以重试整个函数」，未说清是否先回滚。现在明确要求「先完全回滚、
  再以事务前状态重跑」，并补了执行该语义的测试。`settleOne` 先迁移状态后动钱的顺序只有在回滚前提下才安全
- **`AppendCommission` 现在校验金额符号与冲正链接的一致性**：零额记录被拒绝，负数必须带 `ReverseOf`，
  正数不得带。此前可以写入引擎既无法解释也无法对账的记录
- **`AccrueResult.UnallocatedBase`**：明细之和小于实付金额是合法的（运费、赠品），但此前完全无声——
  漏报一个明细与一次成功入账的结果一模一样。现在差额会出现在返回值里
- **重放分支不再返回 `Attributed=true, AgentUserID=0`** 这种域内不存在的组合

### Fixed

- **重复投递的退款会重复冲正**：v0.1 的实现按「本次退款金额」计算冲正，无法区分重投与二次退款；改为按**累计退款额求目标值**，并以退款凭证的幂等键去重
- **`TotalEarned` / `TotalReversed` 不在任何不变量覆盖范围内**：篡改成任意值后自检仍然通过。现由 I3 覆盖
- **`Maintain` 会因一条坏记录停掉全部租户**：改为隔离 + 上报，并按租户隔离失败
- **I2 把冲正记录误判为「没有入账流水」**：流水引用收集现在覆盖 `LedgerAccrue` / `LedgerReverse` / `LedgerVoid`
- **`DueCommissions` 先全量扫描再按租户过滤**：索引改为按 `(租户, 到期时间, ID)` 排序，per-tenant 查询重新回到 O(log n + limit)
- **`SelfCheck` 是 O(n²)**：账户分页每次调用都重新排序全部账户；改为维护有序账户索引
- **回滚会在索引 map 里留下空切片键**：失败事务会持续污染索引，改为「键原本不存在则删除」
- **风控冻结的佣金在解冻后永远不会结算**：收货事件现在也为冻结中的佣金确定到账时间，且冻结不再把佣金移出到期索引
- **`Money(math.MinInt64).String()` 输出乱码**：那正是排查资金异常时会调用的函数
- 到期索引在「先尾部追加、再中间插入」的事务回滚后残留脏条目（v0.1 已修，本版补内部断言）

### Tests

- 退款路径的随机属性测试：25 组种子 × 200 次操作，混合分批退款、整单退款、**故意重投同一个退款单号**、以及使用新单号的真实二次退款；每组结束后必须通过完整自检，并逐分销员核对
  `桶余额 == 净佣金`、`TotalReversed == 冲正明细之和`、`TotalEarned − TotalReversed == 净佣金`
- 事务重试契约测试：一个会「执行 → 回滚 → 再执行」的 Store 包装器，验证引擎在重试下不会重复计数（该契约此前只写在文档里，从未被执行过）
- 并发退款恰好一次、跨租户越权拒绝、隔离不阻塞健康结算、自购不参与分佣等回归测试
- 覆盖率：全仓合并 **86.3%**（`go test -coverpkg=./... ./...`）；未覆盖部分主要是溢出与「余额不足」这类不可达的防御分支

---

## [0.1.0] - 2026-09-14

首个可运行版本：**分佣 + 冻结 + 结算 + 自检**的账务内核，零第三方依赖。

### Added

- 金额与费率：`Money`（`int64` 分）与 `Rate`（万分比）独立类型；128 位中间结果防溢出；三种显式舍入；JSON 拒绝小数 number
- 关系链：`BindAgent`（环检测、层级硬上限、上级不可悄悄变更）、`BindBuyer`（首次优先、有效期、防抢客、可审计换绑）
- 分佣：`OnOrderPaid` 单事务完成归因 → 多级分佣 → 入账 → 写流水；支持按 SKU；运行期配额封顶；类型化的 `SkipCode` 解释每一级为什么没发钱
- 冻结与结算：冻结期入账时快照；`OnOrderReceived` 先到先得；`Maintain` 单一心跳且工作量有上界
- 状态机：佣金单状态迁移表，非法迁移与并发冲突区分对待
- 存储：端口定义在根包；写端口只有追加语义；`store/memory` 具备可串行化隔离、撤销日志回滚、键集分页、到期索引、事务嵌套检测、取消传播
- 自检：I1（余额 == 流水和 + 尾部一致 + 无负余额）、I2（佣金 ↔ 流水双向引用 + 配额）
- 属性测试：40 组种子 × 220 次操作的随机事件序列；并发投递与混合流量在 `-race` 下全绿

### Notes

开发期通过测试发现并修复了三个真实缺陷，均已固化为回归测试：`Rate.String()` 把万分比当小数比例输出、`safemath.Sub(MinInt64, MinInt64)` 误报溢出、到期索引在特定事务回滚后残留脏条目。

另有两条行为约定源自安全审查（ADR-018）：`PaidAt` 必填，且归因要求绑定在支付时刻已存在。
