# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 与 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式。

> **提交信息与代码注释统一使用英文**（面向国际社区与 godoc）。
> 本文档与 PRD、ADR 保持中文，因为它们记录的是设计意图与取舍，读者是维护者。
>
> **API 稳定性承诺**：`v1.0` 之前允许破坏性变更，但必须记录在本文件；`v1.0` 之后遵循语义化版本。

## [0.1.0] - 2026-09-14

**首个公开发布**，也是目前唯一的发布：账户、佣金、退款冲正、风控、提现与打款通道、
四个存储后端（内存 / SQLite / MySQL / PostgreSQL）、四条不变量与自检，全部包含在这一版里。

模块路径 `github.com/darkinno-tech/distledger`，MIT 协议，**零第三方依赖**
（根 `go.mod` 的 `require` 块为空）。

> 发布版本只有 **v0.1.0** 一个。设计文档里出现的 v0.2–v0.6 是**尚未开工的工序编号**
> （可观测性钩子、规则层扩展、异步通道），不是已发布的版本。

### Removed

**破坏性变更**：移除佣金单的 `WITHDRAWN` 状态（ADR-044）。

（移除佣金单的 WITHDRAWN 状态）

- `CommissionWithdrawn` 状态常量、`Settled → Withdrawn` 这条迁移边，
  以及 `reversalDelta` 中针对它的分支
- `SkipAlreadyWithdrawn`（随状态一起失去生产者；跳过的原因不持久化，留空无害）
- **数值 2 被退役而非复用**：状态以整数持久化到 `state` 列，把后面的状态前移会让
  `Reversed` 从 3 变 2，**每个已有数据库都会读错自己的行**。因此保留占位，
  且 `CommissionState.Valid()` **显式排除**它（它落在 `CommissionVoid` 之下，
  单靠范围检查会把它当成合法状态）

#### 迁移

- 调用方若用 `CommissionWithdrawn` 判断「这条佣金是否已付过」，
  应改问账户余额与提现单：库的回答是**问账户**，因为那有唯一答案，而佣金层没有
  （可提现桶里的钱在同质的已结算佣金之间无法唯一归因）
- 数据无需迁移：状态值未变，`state = 2` 若曾存在会被 `Valid()` 判为非法并被自检报出

---



提现与打款通道，以及为「已出账后追回」而设的欠款策略。

### Added（提现与打款通道）

- **提现链路**：`RequestWithdraw` → `ApproveWithdraw` / `RejectWithdraw` → `MarkWithdrawPaid`
  或 `PayWithdraw`。**钱在「申请」时即从可提现桶移入冻结桶**——否则用户可以申请提现后、
  在任何人审核之前把同一笔余额再花一次
- **`PayoutChannel`**：`ManualChannel`（不做资金动作，记录人已线下打款）与 `MockChannel`（测试用，
  可配置失败）。`Config.Payout` 为空时 `PayWithdraw` 返回 `ErrNoPayoutChannel`
  （配置问题、不可重试），而申请 / 审核 / 驳回 / 手动确认全部照常工作——
  未配置的渠道**漏不出钱**，这是刻意的默认（ADR-010）
- **`MinWithdraw` / `FeeRateBP` / `AllowNegative` 三项规则**，现在都有代码读取它们
- **欠款策略（ADR-042）**：`AllowNegative` 默认 `false`，冲正追不回已出账的钱时**拒绝并报出缺口**；
  开启后允许**可提现桶**为负、由后续收益自动抵扣。**冻结桶即便开启也不许为负**——
  那意味着同一笔佣金被扣了两次，是缺陷而非欠款
- **不变量 I4**：提现单状态 ↔ 冻结桶 ↔ 流水三方一致。它抓的是**状态机看不见的那一类**——
  把提现单标记为已打款却没有移动钱，这是一个**合法迁移**，只有钱不见了
- **`InvariantResult.Notes`**：受控欠款进 Notes 而不是 Violations，`OK` 不受影响。
  不这样区分的话，开启 `AllowNegative` 的租户会看到**永久红灯**，真正的违规就无法与预期内的欠款区分
- `dist_withdraw` 表，四个后端均可迁移；`helpers`：新增 `LedgerBizType.referencesWithdrawal`

### Fixed（提现实现过程中发现）

- **幂等键不能内嵌 user id**：`withdrawIdemKey` 起初把 `UserID` 混进哈希，
  而存储的唯一索引是 `UNIQUE(tenant_id, idem_key)`。后果是**调用方按自己的 key 查不回自己的提现单**
  ——公开的 `WithdrawalByIdemKey` 只知道租户。是测试发现的。现在键只与租户和调用方 key 有关，
  与索引覆盖的范围一致
- **重试路径曾经不可达**：`PayWithdraw` 起初只接受 `Approved` 状态，
  于是打款失败后的重试会报非法迁移——钱被冻结着却永远付不出去
- **驳回必须给出理由**：空理由会让代理商问「为什么」时得不到任何答案
- **I4 曾把手工调整流水误报**为「非数字的提现单号」：解析 `BizID` 前必须先按业务类型过滤，
  而手工调整的 `BizID` 是自由文本。修法是在 `LedgerBizType` 上加 `referencesWithdrawal()`，
  与既有的 `referencesCommission()` 对称——「这条流水引用什么 id」只在一处回答
- **i4 的 `Checked` 为 0 被既有测试拦下**：无提现单的租户逐条检查提现单是零项，
  而 `Checked` 存在的意义正是区分「查过了」与「什么都没查」。现在把比对过的账户数也计入

---

**首个公开发布。**

本文件此前的段落是**开发期里程碑**（内核 → 退款与风控 → SQL 存储与多方言），
它们记录了设计推进的过程，但**从未单独打过 tag**。为了让版本历史与 tag 历史一致，
它们保留在本节之下作为分组标题，不再作为独立版本。

模块路径为 `github.com/darkinno-tech/distledger`，MIT 协议，**零第三方依赖**
（根 `go.mod` 的 `require` 块为空）。

### Added（SQL 存储与多方言）

- `store/sql`：可移植 SQL 内核的方言契约与 schema。`Dialect` 只描述**真正因数据库而异**的部分——
  占位符、标识符引用、类型名、自增主键形式、重复键/可重试错误/已存在的判定、索引 DDL
- `store/mysql`、`store/postgres`、`store/sqlite`：三个方言包，各约 100 行，**均不 import 任何驱动**
- 可移植 schema 以 `ColumnKind` 描述一次，所有方言由它渲染：`Schema()` / `Statements(Dialect)`
- 错误分类采用**结构化识别**：按驱动错误暴露的 `Number`/`Code` 字段或 `SQLState()` 方法匹配，
  不 import 驱动包。无法识别时退化为一段有限的短语匹配，且**失败方向是安全的**——未识别的重复键
  变成硬错误，而不是静默重复记账
- 时刻以**整数纳秒**存储而非原生时间类型：原生时间戳的时区语义因库而异，一个会随服务器时区
  改变含义的账本不叫账本。缺省时刻存 NULL 而不是哨兵值

### Added（SQL store 主体与集成验证）

- `store/sql` 的完整实现：26 个端口方法、事务与重试循环、`Migrate`、`Migrate` 幂等、`CheckSchema`
- `store/mysql` / `store/postgres` / `store/sqlite` 各自的 `Open(db)`，库**不打开也不关闭**连接
- **独立模块 `integration/`**：驱动只在这里出现，因此库本体的 `require` 依旧为空。
  `go test ./...`（根目录）不需要数据库；集成测试用环境变量开关，未设置则跳过
- 集成测试对**四个后端跑同一套断言**（68 个子测试），外加完整引擎生命周期、风控、自检、
  事务重试契约与并发幂等

### Fixed（由集成测试在真实数据库上发现）

- **PostgreSQL 下「插入 → 捕获重复键错误」不可用**：PG 里一条语句失败会**中止整个事务**，
  后续语句全部报错、`COMMIT` 变成回滚。新增 `Dialect.InsertConflictClause`，
  PG 用 `ON CONFLICT DO NOTHING` 并从影响行数判断结果；MySQL/SQLite 保持错误分类
- **`NOT NULL` 文本列被写入 NULL**：空串有语义的列（如 `order_item_id` 空串表示整单级分佣）
  是 `NOT NULL`，三个后端在集成测试首轮就同时报错
- **非法状态迁移未被拒绝**：SQL 实现只靠 `WHERE state = ?` 匹配，导致 `Pending -> Withdrawn`
  被直接执行。改为匹配成功后校验合法性并让事务回滚，
  从而与内存实现保持同样的「状态/版本不匹配优先报 Conflict」语义
- **冲正上界的 SQL 守卫方向写反**：原为 `reversed_amount <= 新值`，恒真。
  改为 `amount >= 新值`——上界取决于行自身的金额，只有拿**新值**与行内 `amount` 比较才在并发下正确
- **首次计佣并发时向「正在加钱」的调用方返回「余额不足」（ADR-038，P1）**：
  `IncrementAccount` 把「行存在 + 0 行受影响」读成「守卫拒绝了这次更新」，但这句话还有第二种含义——
  **行在 UPDATE 与 SELECT 之间被另一个事务创建**，此时增量一次都没应用，正确响应是重试而不是报错。
  表象是一个 `+5.00` 的计佣被报成 `ErrInsufficientBalance`，且报出的「会变负的桶」列表是**空的**。
  修法：把「0 行受影响」的三种来源显式分开，两个竞态共用同一个重试响应（增量可交换，重跑一次
  不等于应用两次），仅在增量确实会把某个桶压成负时才拒绝，并且错误信息里带上 delta 与具体桶名。
  实测：修复前 `TestEngineConcurrentOrdersToOneAgent` 10 次失败 6 次，修复后连续 25 次全过。
  复盘见 `docs/fault-reviews/2026-02-11-首次计佣并发时误报余额不足.md`
- **并发测试丢弃了 error，只统计失败数量**：一个说不出原因的失败把排查从「看一眼日志」变成
  「反复重跑碰运气」——上面那条 P1 因此被误当成 flaky test。现在并发测试保留并输出首个错误
- **可观测性钩子**（指标、埋点）与 `examples` 补齐 02 / 03 / 05
- **异步打款通道**（结果在调用时未知）与**代扣税**——见 ADR-043 与 PRD §11

### Changed
- **模块路径改为 `github.com/darkinno-tech/distledger`**，仓库位于
  `github.com/darkinno-tech/distledger`。Go 模块路径必须与仓库位置一致，否则 `go get` 无法解析
- **端口改为按「工作」提问（ADR-035）**：`Reader.Tenants()` → `TenantsWithDueWork(dueAt, limit)` →
  **最终定为 `DueWork(dueAt, limit)`，返回跨租户最早到期的一批佣金**。租户登记表与六条写路径上的
  登记调用一并移除。原因分两层：ADR-034 只把「取租户列表」变便宜了，而 `Maintain` 的真正成本是
  **每个租户一次事务**——5000 租户就是每分钟 5000 次 `BeginTx`，即使没有任何到期项。
  更进一步，「哪些租户有活」这个问题**在 MySQL 上无法高效回答**：实测 80000 条到期记录下，
  `SELECT DISTINCT tenant_id` 无论怎么套窗口都必须读完整个到期集合（12~33 ms，带 Group 节点；
  套了限窗子查询反而更慢，41 ms）。而「最早到期的 N 笔工作」由 `(state, available_at, id)` 提供
  **紧致范围扫描**：无到期项时是一次空查找，有到期项时读满 limit 即停。
  库不拥有租户，因此没有资格枚举租户；它拥有的是工作，就应该问工作。
- **到期索引列序改为 `(state, available_at, id)`**，并保留 `(state, tenant_id, available_at, id)`
  供单租户批处理：以 `tenant_id` 打头会让跨租户查询退化为扫描排在最前面那个租户的全部积压
  （噪声邻居）。列序是承重的，测试直接断言它。
- **索引名由列推导，不再由位置决定（ADR-039）**：`table_idx_3` 这类名字编码的是「索引在列表里排第几」，
  列表一变就换了含义——老库迁移出的 `idx_3` 与新库同名的索引可能是**两组不同的列**。
  改为 `table_idx_state_available_at_id`：名字成为定义的函数，且在 EXPLAIN 里自解释。
- **`Maintain` 工作预算改为共享额度（ADR-040，修正上一条实现）**：预算原先只在外层轮次开头检查，
  而内层单个租户还能再结算 `maxMaintainRounds × maintainBatch = 20000` 笔，
  于是真正的上界是「租户数 × 20000」。实测 30 租户 × 900 笔时一次调用结算了 **27000** 笔。
  现在预算是被两层循环共享的一个对象，只在**事务提交后**记账（store 会重跑闭包做回滚重试），
  **读取也受预算约束**，且**在每个租户之前**检查。

### Added（社区文件与 CI）

- **`CONTRIBUTING.md` / `CONTRIBUTING.zh-CN.md`**：把此前散落且**没有写下来**的规则一次写完——
  两条硬规则（根目录测试不需要数据库、`require` 块保持为空）、语言分工（代码与提交用英文，
  设计文档用中文）、「注释解释为什么」、「测试要固定成本」、「决策要写 ADR」，以及提交规范。
  此前「设计文档用中文」这条分工从未被记录，只能靠观察推断
- **`.github/workflows/ci.yml`**：把两条硬规则**机械地**检查掉，而不是靠 review 记得——
  gofmt、vet、断言 `go.mod` 没有 `require` 块、断言驱动包无法从根模块抵达
  （`integration/` 是独立模块，两半都断言），并在**刻意清空 DSN 环境变量**的情况下跑根测试。
  另一个 job 起 MySQL 与 PostgreSQL service container，对四个后端跑同一套断言
- **Issue 模板**（缺陷 / 需求）：缺陷模板按这个库的特点问问题——哪些金额错了、
  哪个存储后端、能否用 `store/memory` 复现、以及 `SelfCheck` 输出。
  需求模板要求先回答「这落在分界线的哪一侧」
- **`.github/pull_request_template.md`**：要求说清改了什么契约、带一个**改动前会失败**的测试、
  以及性能改动测到了什么
- **`SECURITY.md`**：明确列出这个库语境下什么算安全问题（账务完整性、租户隔离、整数运算、
  幂等、SQL 注入、无界工作、依赖面），以及什么**不是**（库不做合规判定、不鉴权、不加密存储）
- **`CODE_OF_CONDUCT.md`**

### Fixed（CI 首次运行暴露的问题）

- **`integration/` 需要 Go 1.25，而这一点没有任何地方写明**：库声明 Go 1.22，
  集成模块声明 Go 1.25，两者不一致但**都是对的**——`modernc.org/sqlite` 与
  `jackc/pgx/v5` 自身就要求 1.25。此前靠 `setup-go@v5` 让 Go 静默下载新工具链才没暴露；
  升到 v6（它固定 `GOTOOLCHAIN=local`）后 CI 立刻报
  `go.mod requires go >= 1.25.0 (running go 1.22.12)`。
  这个错误一个 Go 1.22 的贡献者一定会撞上，且报错命令看起来毫不相关。
  已在 `integration/go.mod`、CI、两份 README 与两份 CONTRIBUTING 中写明：
  **用这个库只需要 1.22，跑集成测试需要 1.25**
- **CI 的两个警告**：`actions/checkout` 与 `actions/setup-go` 的 v4/v5 面向已弃用的
  Node 20，升到 v6；库的 job 关闭模块缓存——**库没有 `go.sum`，因为它没有依赖**，
  缓存没有键可用，这个警告会永远重复（集成模块有 `go.sum`，缓存保留）

### Fixed（文档与实际不符）

- **中文 README 里的示例输出是编造的**：它显示的是翻译成中文的输出（`③ 归因成功=true`），
  而示例程序实际输出英文；`I2` 标成「检查 1 项」（实际是 2），且**整个 I3 行都不存在**。
  已替换为**实际输出**并加了一句说明。两份 README 引用的输出现在都与程序逐行一致，并已用脚本核对
- **中文 README 的贡献清单已过时**：仍把 PostgreSQL / SQLite 列为「待实现的 Store」，
  而它们已经实现；「30 秒不需要 MySQL」的标题同样过时

### Added（自检下推、不变量文档与故障复盘）
- **`Reconciler`：可选的对账下推接口（ADR-041）**。`SelfCheck` 的 I1 原先逐行遍历账户、
  对每个账户把全部流水分页读回客户端；10 亿行流水意味着 10 亿行过网、约 100 万次往返，
  为了在正常情况下得出「没有问题」要跑数小时。现在 SQL 存储用自己的聚合完成比较，
  只把**可能不一致的账户**交回（正常为 0 行），**是否违规仍由库判定**——
  存储层只回答「哪些可能不对」。判断只有一份实现（`accountConservationViolations`），
  两条路径共用。内存实现不实现它，退回逐行；**没实现就是逐行检查，绝不是不检查**
- **对账 SQL 由 `AllBuckets()` 生成**，不是手写四个列名。手写等于在存储层放下桶定义的
  第二个副本，加第五个桶时它不会报错——自检会继续通过，只是覆盖变少
- **`Report.Reconciled`**：说明 I1 走的是哪条路径（两条路径成本差一个量级，
  「检查通过」的含义不同）
- **`docs/invariants.md`**：逐条说明 I1/I2/I3 各保证什么、由什么强制、以及**不覆盖什么**。
  最后一项写得比保证更详细，因为「自检通过」最容易被误读成「账目正确」
- **`docs/fault-reviews/`**：故障复盘。首篇记录「首次计佣并发时误报余额不足」
- **`docs/scale.md`：亿级规模审计**。逐热路径给出复杂度、成本估算与结论，区分「已修复」「已定方案未实现」
  「可接受并说明理由」，并诚实声明哪些结论尚未用真实数据库验证
- **成本性质测试**：`TestMaintainCostIsIndependentOfIdleTenants` 直接数操作次数——
  1/50/500 个空闲租户、无到期项时必须是「1 次读、0 次事务」。
  结算结果在两种实现下相同，因此只有数操作次数才能固定这个性质

### 退款冲正与风控

退款冲正、风控冻结与作废，以及一轮对抗性审查发现问题的修复。

#### Added

**退款冲正**
- `OnOrderRefunded`：支持整单全额（`IsFull`）、按明细（`Items`）、整单部分（`Amount`）三种表达，三者互斥
- 冲正以**负额佣金记录**追加，`Commission.ReverseOf` 指回原始记录；原始记录的财务字段保持不可变
- `Commission.ReversedAmount`：已被冲正的累计额（生命周期字段，带版本 CAS）
- `Account.TotalReversed`：累计冲正额，与 `TotalEarned` 配对得到净收益
- 按明细、按批次、任意顺序的退款；单次退款可跨多个计佣单位按基数占比自动分摊
- `Refund` 退款凭证实体：记录每次退款批次、金额与**该明细的累计退款额**
- 不变量 **I3**：冲正累计额 ↔ 冲正明细 ↔ 账户毛额/冲正额三方对账
- `Bucket` 类型：资金桶的唯一一处定义，被负余额守卫、I1 与流水构造共同使用

**风控**
- `FreezeCommission` / `UnfreezeCommission`：让 `Frozen` 状态真正可达（此前只存在于迁移表中）
- `VoidCommission`：没收一笔佣金——全额追回并落到终态 `Void`，流水业务类型为 `LedgerVoid`
- 状态机新增 `Frozen → Reversed`：风控冻结中的佣金，其钱仍在待结算桶里，退款时必须能拿回来

**运维**
- `MaintainResult.Quarantined`：数据异常的佣金被隔离并上报，而不是中止整批结算
- `Maintain` 按租户隔离失败：一个租户出错不再阻塞其它租户
- `Logger` 真正被使用（冻结、解冻、作废、心跳汇总、隔离告警），此前是零调用点的空壳

#### Changed（破坏性）

- **`Maintain(ctx)`**：去掉 `now` 参数。处理时间一律来自注入的 `Clock`——允许调用方推进时间就等于允许他们提前结算。事件时间（`PaidAt`、`ReceivedAt`、`RefundedAt`）仍由调用方提供，因为那是业务事实（见 ADR-023）
- **`OrderRefundedEvent.IdemKey` 必填**：没有它，「同一次退款被重投」与「两次金额相同的真实退款」在数据上完全等价（见 ADR-024）
- **`EligibilityChecker.Eligible` 改签名**为 `Eligible(ctx, EligibilityInput)`，输入中新增 `BuyerUserID`，使自购判定可以被表达
- **`AccrueResult.AccruedAmount()` 返回 `(Money, error)`**，不再静默回绕
- 明细金额必须 > 0（0 元明细既不能产生佣金，又曾是最廉价的放大工作量手段）
- 同一订单号的第二个支付事件若声称**不同的买家**，现在返回错误而不是被当成重放成功

#### Fixed（审查驱动的结构性修复）

- **`TotalEarned` / `TotalReversed` 落在不变量之外**：早期每个检查都手写字段名，导致新资金字段
  静默地不受任何约束。引入 `Bucket` 类型把资金桶定义收敛到唯一一处，`Settleable()` 与 I1 都遍历它，
  流水构造也由它驱动；另有一个反射测试强制「`Account` 上每个 `Money` 字段要么是桶、要么登记在豁免表里」
- **重试的定义被写死**：原文只说「可以重试整个函数」，未说清是否先回滚。现在明确要求「先完全回滚、
  再以事务前状态重跑」，并补了执行该语义的测试。`settleOne` 先迁移状态后动钱的顺序只有在回滚前提下才安全
- **`AppendCommission` 现在校验金额符号与冲正链接的一致性**：零额记录被拒绝，负数必须带 `ReverseOf`，
  正数不得带。此前可以写入引擎既无法解释也无法对账的记录
- **`AccrueResult.UnallocatedBase`**：明细之和小于实付金额是合法的（运费、赠品），但此前完全无声——
  漏报一个明细与一次成功入账的结果一模一样。现在差额会出现在返回值里
- **重放分支不再返回 `Attributed=true, AgentUserID=0`** 这种域内不存在的组合

#### Fixed

- **重复投递的退款会重复冲正**：早期实现按「本次退款金额」计算冲正，无法区分重投与二次退款；改为按**累计退款额求目标值**，并以退款凭证的幂等键去重
- **`TotalEarned` / `TotalReversed` 不在任何不变量覆盖范围内**：篡改成任意值后自检仍然通过。现由 I3 覆盖
- **`Maintain` 会因一条坏记录停掉全部租户**：改为隔离 + 上报，并按租户隔离失败
- **I2 把冲正记录误判为「没有入账流水」**：流水引用收集现在覆盖 `LedgerAccrue` / `LedgerReverse` / `LedgerVoid`
- **`DueCommissions` 先全量扫描再按租户过滤**：索引改为按 `(租户, 到期时间, ID)` 排序，per-tenant 查询重新回到 O(log n + limit)
- **`SelfCheck` 是 O(n²)**：账户分页每次调用都重新排序全部账户；改为维护有序账户索引
- **回滚会在索引 map 里留下空切片键**：失败事务会持续污染索引，改为「键原本不存在则删除」
- **风控冻结的佣金在解冻后永远不会结算**：收货事件现在也为冻结中的佣金确定到账时间，且冻结不再把佣金移出到期索引
- **`Money(math.MinInt64).String()` 输出乱码**：那正是排查资金异常时会调用的函数
- 到期索引在「先尾部追加、再中间插入」的事务回滚后残留脏条目（已修，并补了内部断言）

#### Tests

- 退款路径的随机属性测试：25 组种子 × 200 次操作，混合分批退款、整单退款、**故意重投同一个退款单号**、以及使用新单号的真实二次退款；每组结束后必须通过完整自检，并逐分销员核对
  `桶余额 == 净佣金`、`TotalReversed == 冲正明细之和`、`TotalEarned − TotalReversed == 净佣金`
- 事务重试契约测试：一个会「执行 → 回滚 → 再执行」的 Store 包装器，验证引擎在重试下不会重复计数（该契约此前只写在文档里，从未被执行过）
- 并发退款恰好一次、跨租户越权拒绝、隔离不阻塞健康结算、自购不参与分佣等回归测试
- 覆盖率：全仓合并 **86.3%**（`go test -coverpkg=./... ./...`）；未覆盖部分主要是溢出与「余额不足」这类不可达的防御分支

---

#### 内核

首个可运行版本：**分佣 + 冻结 + 结算 + 自检**的账务内核，零第三方依赖。

#### Added

- 金额与费率：`Money`（`int64` 分）与 `Rate`（万分比）独立类型；128 位中间结果防溢出；三种显式舍入；JSON 拒绝小数 number
- 关系链：`BindAgent`（环检测、层级硬上限、上级不可悄悄变更）、`BindBuyer`（首次优先、有效期、防抢客、可审计换绑）
- 分佣：`OnOrderPaid` 单事务完成归因 → 多级分佣 → 入账 → 写流水；支持按 SKU；运行期配额封顶；类型化的 `SkipCode` 解释每一级为什么没发钱
- 冻结与结算：冻结期入账时快照；`OnOrderReceived` 先到先得；`Maintain` 单一心跳且工作量有上界
- 状态机：佣金单状态迁移表，非法迁移与并发冲突区分对待
- 存储：端口定义在根包；写端口只有追加语义；`store/memory` 具备可串行化隔离、撤销日志回滚、键集分页、到期索引、事务嵌套检测、取消传播
- 自检：I1（余额 == 流水和 + 尾部一致 + 无负余额）、I2（佣金 ↔ 流水双向引用 + 配额）
- 属性测试：40 组种子 × 220 次操作的随机事件序列；并发投递与混合流量在 `-race` 下全绿

#### Notes

开发期通过测试发现并修复了三个真实缺陷，均已固化为回归测试：`Rate.String()` 把万分比当小数比例输出、`safemath.Sub(MinInt64, MinInt64)` 误报溢出、到期索引在特定事务回滚后残留脏条目。

另有两条行为约定源自安全审查（ADR-018）：`PaidAt` 必填，且归因要求绑定在支付时刻已存在。
