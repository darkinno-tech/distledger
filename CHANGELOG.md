# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 与 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式。

> **API 稳定性承诺**：`v1.0` 之前允许破坏性变更，但必须记录在本文件；`v1.0` 之后遵循语义化版本。

---

## [Unreleased]

### Planned
- v0.3 退款冲正（全退 / 部分退 / 已提现追回，以及不变量 I3）
- v0.4 MySQL Store（事务、行锁、`Migrate`、`schema.sql`）
- v0.5 提现链路与 `PayoutChannel`
- v0.6 可观测性钩子

---

## [0.1.0] - 2026-09-14

首个可运行版本：**分佣 + 冻结 + 结算 + 自检**的账务内核，零第三方依赖。

### Added

**金额与费率**
- `Money`（`int64` 最小货币单位）与 `Rate`（万分比 `int32`）两个独立类型，编译器阻止单位混用
- `Money.Apply` 使用 128 位中间结果做乘除，**溢出返回错误而不是回绕**
- 三种显式舍入模式（`RoundDown` / `RoundHalfUp` / `RoundUp`），默认截断
- `Money` 的 JSON 序列化为十进制字符串；**拒绝解析小数 number**，防止 JS 侧精度丢失
- `ParseMoney` 对超过两位小数直接报错，不静默截断

**关系链**
- `BindAgent`：父指针 + 深度，含环检测、`MaxLevels` 深度上限、上级不可悄悄变更
- `BindBuyer`：首次优先的归因绑定、有效期、防抢客（`ErrBindingLocked`）、可审计换绑（`ReboundFrom`）

**分佣**
- `OnOrderPaid`：归因 → 多级分佣 → 入账 → 写流水，单事务完成
- 支持按 SKU 分佣；**明细金额之和超过实付金额直接拒绝**（资损入口）
- 幂等键由库派生（长度前缀哈希，抗拼接歧义）；调用方自定义键作为**种子**参与派生
- 运行期配额封顶（不变量 I2 的第二道防线）
- `AccrueResult.Skipped` 用类型化 `SkipCode` 解释每一级为什么没发钱

**冻结与结算**
- 冻结天数在入账时**快照**进佣金单，配置变更不追溯影响历史
- `OnOrderReceived` 先到先得，重复投递不能把到账时间往前刷
- `Maintain`：单一心跳，内部批处理，工作量有上界，可跨租户

**状态机**
- 佣金单 6 状态迁移表，非法迁移返回 `ErrIllegalTransition`，并发冲突返回 `ErrConflict`（两者区分开，后者可重试）

**存储**
- `Store` 端口定义在根包（接口由消费方定义，避免导入环）
- 写端口**只提供追加语义**：`AppendCommission` / `AppendLedger`，不存在通用 `Update`。账本不可变由接口形状保证
- `store/memory`：全局写锁串行化（可串行化隔离）、撤销日志回滚、键集分页、O(log n) 到期索引、事务嵌套检测、取消传播

**自检**
- `SelfCheck`：I1（余额 == 流水和 + 尾部一致 + 无负余额）与 I2（佣金↔流水双向引用 + 配额），带 `Checked` 计数与限量违规样本
- 可选接口 `SchemaChecker` / `ReportedStore`

**测试**
- 随机事件序列属性测试（40 组种子 × 220 次操作），每组结束后必须通过完整自检
- 并发投递 / 并发心跳 / 混合流量测试，全部在 `-race` 下通过
- 到期索引回滚的**内部**测试：直接比对索引快照，而不是只看行为
- 覆盖率：根包 88.7% · `store/memory` 93.5% · `internal/safemath` 92.9%

### Notes
- 开发过程中通过测试发现并修复了三个真实缺陷，均已固化为回归测试：
  1. `Rate.String()` 把万分比当成小数比例输出（`500bp` 显示成 `0.05%`）
  2. `safemath.Sub(MinInt64, MinInt64)` 被误报为溢出（对合法运算报告失败）
  3. 到期索引在「先尾部追加、再中间插入」的事务回滚后残留脏条目
- 另有两条行为约定源自实现期的安全审查，见 ADR-018：`PaidAt` 改为必填，且归因要求绑定在支付时刻已存在。
- v0.1 **不含**退款冲正与提现，因此 `Rules` 中与提现相关的字段（起提门槛、手续费率等）尚未引入——不留空壳配置项。

---

## 版本规划（摘自 PRD §13）

| 版本 | 范围 |
|---|---|
| v0.1 ✅ | 关系链 + 归因 + 多级分佣 + 幂等 + 冻结快照 + `Maintain` 结算 + 内存 Store + 自检（I1/I2） |
| v0.3 | 退款冲正（全退/部分退/已结算追回）+ I3 |
| v0.4 | MySQL store + 事务/行锁 + `Migrate` |
| v0.5 | 提现链路 + `PayoutChannel` + `ManualChannel` |
| v0.6 | 可观测性钩子 + `examples` 补齐 02–05 |
| v1.0 | API 冻结 + 文档完整 + 属性测试长期稳定 |
